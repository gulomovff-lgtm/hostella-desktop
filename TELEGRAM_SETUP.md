# 🔐 Настройка Cloud Functions для Telegram

## Локальная разработка

В папке `functions/` используйте файл `.env.local` (уже содержит токен локально).

**ВАЖНО:** Никогда не коммитируйте `.env.local` с реальным токеном!

## Продакшн (Firebase Console)

Когда будете деплоить на Firebase:

### Способ 1: Через Firebase CLI (рекомендуется)

```bash
firebase functions:config:set telegram.bot_token="ВАШ_РЕАЛЬНЫЙ_ТОКЕН"
```

Затем в коде функции используйте:
```javascript
const botToken = process.env.TELEGRAM_BOT_TOKEN;
```

### Способ 2: Через Firebase Console UI

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Перейдите в `Проект` → `Параметры проекта`
3. Вкладка `Функции`
4. Создайте переменные окружения

## Безопасность ✅

- ✅ API токен **не светится в коде**
- ✅ Не попадает в Git репо
- ✅ Защищен проверкой аутентификации на Firebase
- ✅ Хранится только на сервере Firebase

## Деплой

```bash
firebase deploy --only functions
```

Функция `sendTelegramMessage` будет доступна после деплоя.

